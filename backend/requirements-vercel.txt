# ==========================================
# VERCEL FREE TIER OPTIMIZED (~100MB total)
# 250MB limit - this deployment: ~100-120MB
# ==========================================

fastapi>=0.115.0            # Updated for anyio>=4.5 compatibility
pydantic>=2.10.3
python-dotenv==1.0.0

# AI: Cloud-only (no local models)
google-generativeai==0.3.2  # Google Gemini embeddings + LLM
openai>=1.87.0              # API client (required by openai-agents)
openai-agents==0.1.0        # Lightweight orchestration

# Data Storage (clients only)
qdrant-client==1.7.1         # Vector DB cloud access
psycopg2-binary==2.9.10      # Postgres client

# Utilities
httpx>=0.27.0              # Required by mcp>=0.27
tiktoken==0.5.2
python-multipart==0.0.6

# ❌ Removed: litellm, asyncpg (not needed for serverless)
# ❌ NO: torch, transformers, sentence-transformers (too heavy)
